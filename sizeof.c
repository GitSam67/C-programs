#include<stdio.h>

int main(){
    printf("The size of int operator in my system architecture is %d\n", sizeof(int));
    printf("The size of float operator in my system architecture is %d\n", sizeof(float));
    printf("The size of char operator in my system architecture is %d\n", sizeof(char));
    return 0;
}